{
  "entities": {
    "SharedResume": {
      "title": "SharedResume",
      "description": "Snapshot of a user's resume for public sharing.",
      "type": "object",
      "properties": {
        "createdAt": {
          "type": "number",
          "description": "Timestamp"
        }
      }
    }
  },
  "firestore": {
    "/sharedResumes/{resumeId}": {
      "schema": {
        "$ref": "#/entities/SharedResume"
      },
      "description": "Public resume snapshots."
    }
  }
}
